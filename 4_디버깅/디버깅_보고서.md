# 4. 디버깅 보고서 (10점)

> 💡 **디버깅이란?** 프로그램에서 발생한 오류(버그)를 찾아내고 고치는 과정입니다.

---

## 📝 작성 정보
- **작성 날짜:** 2025년 _11_월 _20_일
- **작성자:** (이름)
송민지
---

## 🐛 발견한 오류 #1

### 1) 어떤 문제가 발생했나요?
(예: 프로그램을 실행하면 이상한 값이 나왔어요 / 컴파일이 안 돼요)
컴파일이 안됐다. 


### 2) 오류가 발생한 코드
```c
// 문제가 있던 코드를 여기에 복사하세요
```
	if(r==1){ p = &city; name = "도시";}
	else if (r==2){p = &forest; name = "숲"}
	else if (r==3){p = &ocean; name = "바다"}
	else {printf("잘못된 선택\n"); return 0;}

### 3) 왜 오류가 생겼나요?
(예: 세미콜론(;)을 빼먹었어요 / 변수 이름을 잘못 썼어요)
세미콜론을 적지 않았다.
```
원인을 간단히 적어보세요.
```

### 4) 어떻게 고쳤나요?
```c
// 고친 코드를 여기에 적어주세요
```
	if(r==1){ p = &city; name = "도시";}
	else if (r==2){p = &forest; name = "숲";}
	else if (r==3){p = &ocean; name = "바다";}
	else {printf("잘못된 선택\n"); return 0;}

### 5) 고친 후 결과
- [ 0 ] ✅ 잘 작동해요!
- [ ] ❌ 아직 문제가 있어요

---

## 🐛 발견한 오류 #2

### 1) 어떤 문제가 발생했나요?
선택한 지역별로 다른 선택지가 나오게 하고 싶다.

### 2) 오류가 발생한 코드
#include <stdio.h>
int main()
{
	double city = 50, forest = 50, ocean = 50;
	int r, a;
	double *p;
	const char *name;
	
	printf("1. 도시 2. 숲 3. 바다\n");
	scanf("%d", &r);
	
	if(r==1){ p = &city; name = "도시";}
	else if (r==2){p = &forest; name = "숲";}
	else if (r==3){p = &ocean; name = "바다";}
	else {printf("잘못된 선택\n"); return 0;}

	printf("%s 선택 (현재 오염도 %.1f)\n", name, *p);
	
	if (r==1)
	{
	printf("1. 친환경  2. 아무것도 안하기  3. 환경 오염\n");
	scanf("%d", &a);

	
	if (a==1) *p -= 10;
	else if (a==3) *p += 15;

	if (*p < 0)*p = 0;
	if (*p > 100)*p = 100;
	
	printf ("행동 후 %s 오염도 : %.1f\n", name, *p);
	
	if (*p <=40) printf ("당신은 지구를 생각하는 진정한 지구인!\n");
	else if (*p <= 60) printf("지구를 위해 노력해주세요!\n");
	else printf ("지구 온난화가 심해졌어요!\n");
	
	return 0;
}

### 3) 왜 오류가 생겼나요?
미지수를 하나만 선언했다. (a)

### 4) 어떻게 고쳤나요?
```c
// 고친 코드를 여기에 적어주세요
```
#include <stdio.h>
int main()
{
	double city = 50, forest = 50, ocean = 50;
	int r, a, b, c;
	double *p;
	const char *name;
	
	printf("1. 도시 2. 숲 3. 바다\n");
	scanf("%d", &r);
	
	if(r==1){ p = &city; name = "도시";}
	else if (r==2){p = &forest; name = "숲";}
	else if (r==3){p = &ocean; name = "바다";}
	else {printf("잘못된 선택\n"); return 0;}

	printf("%s 선택 (현재 오염도 %.1f)\n", name, *p);
	
	if (r==1)
	{
	printf("1. 자전거 타기  2. 대중교통 타기  3. 자가용 차 타기\n");
	scanf("%d", &a);
	}
	else if (r==2)
	{
	printf("1. 나무 심기  2. 그냥 가기  3. 벌목\n");
	scanf("%d", &b);
	}
	else if (r==3)
	{
	printf("1. 쓰레기 줍기  2. 낚시  3. 쓰레기 투기\n");
	scanf("%d", &c);
	}
	
	
	
	if (a==1) *p -= 10;
	else if (a==3) *p += 15;
	
	if (b==1) *p -= 10;
	else if (b==3) *p += 15;
	
	if (c==1) *p -= 10;
	else if (c==3) *p += 15;
	
	if (*p < 0)*p = 0;
	if (*p > 100)*p = 100;
	
	printf ("행동 후 %s 오염도 : %.1f\n", name, *p);
	
	if (*p <=40) printf ("당신은 지구를 생각하는 진정한 지구인!\n");
	else if (*p <= 60) printf("지구를 위해 노력해주세요!\n");
	else printf ("지구 온난화가 심해졌어요!\n");
	
	return 0;
	
}

### 5) 고친 후 결과
- [ ] ✅ 잘 작동해요!
- [ 0 ] ❌ 아직 문제가 있어요

---

## 🐛 발견한 오류 #3 (더 있으면 추가로 작성하세요)

### 1) 어떤 문제가 발생했나요?
```
문제 상황을 간단히 설명하세요.
```

b와 c를 선택했을 때 값이 이상하게 나왔다.

### 2) 오류가 발생한 코드
```c
// 문제가 있던 코드
```
	double city = 50, forest = 50, ocean = 50;
	int r, a, b, c;
	double *p;
	const char *name;

### 3) 왜 오류가 생겼나요?
```
원인을 간단히 적어보세요.
```
초기화하지 않아서 랜덤 값이 들어갔다.

### 4) 어떻게 고쳤나요?
```c
// 고친 코드
```
	double city = 50, forest = 50, ocean = 50;
	int r, a=0, b=0, c=0;
	double *p;
	const char *name;

### 5) 고친 후 결과
- [ 0 ] ✅ 잘 작동해요!
- [ ] ❌ 아직 문제가 있어요

---

## 🧪 프로그램 테스트 결과

### 테스트 1
- **무엇을 테스트했나요?** 1번 선택
- **어떻게 테스트했나요?** 1번 선택 누름 
- **결과:** ✅ 성공 

### 테스트 2
- **무엇을 테스트했나요?** 2번 선택 
- **어떻게 테스트했나요?** 2번 선택 누름 
- **결과:** ✅ 성공

---

## 💭 디버깅하면서 배운 점

이번 프로젝트에서 오류를 고치면서 새롭게 알게 된 점이나 느낀 점을 자유롭게 적어보세요.

```
세미콜론을 빼먹지 않도록 조심해야겠다.
추가하고 싶은 값이 있으면 변수를 꼭 추가해야된다는 점을 알게되었다. 
값을 초기화 시키지 않으면 그 값 안에 랜덤의 수가 들어간다는 점을 알게되었다. 초기화의 중요성을 느꼈다.

---

## ✅ 최종 체크리스트

프로그램이 잘 작동하는지 최종 확인해보세요!

- [ 0 ] 컴파일이 잘 되나요? (오류 메시지가 없나요?)
- [ 0 ] 프로그램이 실행되나요?
- [ 0 ] 메뉴가 제대로 나오나요?
- [ 0 ] 입력한 값이 정상적으로 처리되나요?
- [ 0 ] 모든 기능이 잘 작동하나요?
